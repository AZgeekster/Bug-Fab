"""Alembic migration environment for the Bug-Fab SQL storage backends.

The migration env.py + alembic.ini live in this subpackage so the migration
scripts ship with the code that defines the schema. See ``alembic.ini`` for
how to point ``alembic`` at this directory from a consumer project.
"""

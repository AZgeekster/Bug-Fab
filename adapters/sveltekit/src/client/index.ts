// Client-side entry point for `bug-fab-sveltekit/client`.
//
// The Svelte component is published as `BugFab.svelte` via `svelte-package`,
// which emits both the source `.svelte` file and pre-compiled outputs.
// Consumers import the default export.
import BugFab from './BugFab.svelte';
export default BugFab;
export { BugFab };
